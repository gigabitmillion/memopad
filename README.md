# Memo Pad

A simple Flutter memo pad application.

This project provides a minimal example of a note taking app implemented in Flutter. Notes are stored locally using `shared_preferences`.

## Running

Ensure you have Flutter installed. Then run:

```bash
flutter pub get
flutter run
```
